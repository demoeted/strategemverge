<div class="is-flex">
  <%= render partial: "layouts/sidenav",
              object: @categories %>

  <%= form_with(url: optical_drives_path, method: :get, local: true, class: "") do |form| %>
    <%= form.select :filter, options_for_select([
      ['All', 'all'],
      ['New', 'new'],
      ['Recently Updated', 'recently updated']
    ]), {}, class: ""%>
    <%= form.submit 'Search', class: "" %>
  <% end %>

  <main class="box" >
    <% @optical_drives.each_with_index do |optical_drive, index| %>
      <% if index % 5 == 0 %>
          <section class="columns">
              <%= render partial: "layouts/product",
                          object: optical_drive %>
      <% else %>
              <%= render partial: "layouts/product",
                          object: optical_drive %>
      <% end %>
      <% if index % 5 == 4 %>
          </section>
      <% end %>
    <% end %>
    <%= paginate @optical_drives %>
  </main>
</div>