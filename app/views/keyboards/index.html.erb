<div class="is-flex">
  <%= render partial: "layouts/sidenav",
              object: @categories %>

  <%= form_with(url: keyboards_path, method: :get, local: true, class: "") do |form| %>
    <%= form.select :filter, options_for_select([
      ['All', 'all'],
      ['New', 'new'],
      ['Recently Updated', 'recently updated']
    ]), {}, class: ""%>
    <%= form.submit 'Search', class: "" %>
  <% end %>

  <main class="box" >
    <% @keyboards.each_with_index do |keyboard, index| %>
      <% if index % 5 == 0 %>
          <section class="columns">
              <%= render partial: "layouts/product",
                          object: keyboard %>
      <% else %>
              <%= render partial: "layouts/product",
                          object: keyboard %>
      <% end %>
      <% if index % 5 == 4 %>
          </section>
      <% end %>
    <% end %>
    <%= paginate @keyboards %>
  </main>
</div>