<aside class="menu" >
    <p class="menu-label">Filter</p>
    <ul class="menu-list">
        <% @categories.each do |category| %>
            <li><%= link_to category.name, polymorphic_url(category) %></li>
        <% end %>
    </ul>
</aside>