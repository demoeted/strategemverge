<aside class="menu" >
    <p class="menu-label">Filter</p>
    <ul class="menu-list">
        <% @categories.each do |category| %>
            <li><%= link_to category.name, "/#{category.name.downcase.tr(" ", "_")}" %></li>
        <% end %>
    </ul>
</aside>